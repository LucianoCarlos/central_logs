language: python
python:
  - '3.7'

install:
  - pip install -r requirements.txt

stages:
  - name: test

jobs:
  include:
    - stage: test
      name: Unit Tests
      script: python manage.py test
